{
  "name": "cf-status-image",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler pages deploy ./public"
  },
  "dependencies": {
    "satori": "^0.10.0",
    "@resvg/resvg-wasm": "^2.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
